export const kzLayout = {
  hero: {
    titleClass:
      "text-[clamp(2.7rem,10vw,5.7rem)] sm:text-[clamp(3.4rem,7.5vw,5.8rem)] lg:text-[clamp(3.8rem,5.7vw,5.8rem)]",
  },

  features: {
    titleClass:
      "text-[clamp(2.3rem,10vw,5.5rem)]",
  },

  paragraph: {
    maxWidth: "100%",
  },
} as const;